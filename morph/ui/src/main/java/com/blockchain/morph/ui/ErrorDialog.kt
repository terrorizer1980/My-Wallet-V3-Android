package com.blockchain.morph.ui

import com.blockchain.morph.ui.homebrew.exchange.ExchangeMenuState

internal fun showErrorDialog(error: ExchangeMenuState.ExchangeMenuError) {
    // TODO(AND-2059) - launch error dialog
}